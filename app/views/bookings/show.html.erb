<h1>報名資訊</h1>
<div>
  <%= link_to '取消報名', activity_booking_path(@booking), data: { turbo_method: 'delete',
                                                                 turbo_confirm: '確定要取消報名嗎？' } %>
</div>
<article>
  <h2>活動主題：<%= @booking.activity.title %></h2>
  <h3>主辦單位：<%= @booking.activity.organizer %></h3>
  <h4>活動地點：<%= @booking.activity.location %></h4>
  <div>報名人數：目前人數 / <%= @booking.activity.max_participants %></div>
  <div>開始時間：<%= @booking.activity.start_time %> － 結束時間：<%= @booking.activity.end_time %></div>
  <p>詳細內容：<%= @booking.activity.description %></p>
</article>

